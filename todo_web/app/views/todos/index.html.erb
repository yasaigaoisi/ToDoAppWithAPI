<h1>Todo</h1>

<ul>
  <% @todos.each do |todo| %>
    <li>
      <%= todo["title"] %>
      <%= link_to "view", todo_path(id: todo["id"]) %>
    </li>
  <% end %>
</ul>

<%= link_to "New", new_todo_path %>
